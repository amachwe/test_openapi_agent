# test_openapi_agent
Tests the Google ADK OpenAPIToolset tool that automatically wraps OpenAPI endpoints into tools useable by ADK Agents. It just requires the API endpoint spec in JSON or YAML format.
